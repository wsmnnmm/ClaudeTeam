# T-67 章节真实证据：第九章 API 超市

## 一句话结论

结论：只到壳层/需授权。当前本机公开访问 `https://scys.com/deepsea/2001/course/164?chapterId=10593` 可以返回 HTML，但没有进入第九章正文，也没有显示课程名、章节标题或正文内容；需要拥有深海圈课程授权登录态的一方提供浏览器会话或截图后，才能补真实正文证据。

## 实际核验动作

- 任务卡：已读取 `T-67`，确认目标是核验第九章《去API超市进货：给你的产品加上现成的AI超能力》，不能凭目录、标题或旧记忆假装看过。
- HTTP 探测：`curl -L -I` 访问目标 URL 返回 `HTTP 200`，有效 URL 仍为目标章节链接，`content-type=text/html; charset=utf-8`。
- HTML 源码核验：页面标题仅为 `scys.com`，正文只看到 `div id="app" v-cloak` 等 SPA 壳层；未检出 `去API`、`OpenRouter`、`Replicate`、`fal`、`火山`、`chapterId=10593` 等章节正文标识。
- 浏览器截图：使用 Chrome headless 直接访问目标 URL 并截图，结果为空白浅灰页面，没有课程名、章节目录或正文区域。

## 截图证据

- 截图路径：`knowledge-base/course-zone/screenshots/2026-05-20-course-164-chapter-10593-real-or-blocked.png`
- 截图结论：空白壳层，不足以证明已进入章节正文。

## 可确认与不可确认

### 可确认

- 公开无登录态访问目标 URL 不报 404，服务端返回 SPA 页面。
- 当前访问结果没有呈现第九章正文内容。
- 老板提供的目录结构只能作为外部线索，不能替代真实课程正文证据。

### 不可确认

- 不能确认第九章正文是否实际讲解火山引擎、OpenRouter.ai、fal.ai、Replicate 的具体选择和调用方法。
- 不能确认课程中“实操1 替换API”和“实操2 用 TRAE 做虚拟试衣”的具体步骤。
- 不能基于当前空白壳层产出章节摘要或教学结论。

## 下一步需要

- 需要有课程授权登录态的浏览器会话、老板提供的真实课程截图，或临时授权后重新访问。
- 补证时必须遮挡头像、手机号、二维码、cookie、token、账号信息等敏感内容。
